server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Sateesh079/microservices-config
          clone-on-start: true

# Optional: refresh delay between checks
  application:
    name: config-server
